FROM python:3.11-slim

WORKDIR /app

# Install uv
RUN pip install uv --quiet

COPY pyproject.toml .
RUN uv pip install --system -e .

COPY . .

# Start command is provided per-service in docker-compose / Railway
CMD ["python", "-m", "agents.scout.agent"]
