FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

ENV PYTHONPATH=/app

# Start command is overridden per-service in docker-compose / Railway
CMD ["python", "agents/scout/agent.py"]
